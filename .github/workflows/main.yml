name: Build APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      # 🔥 JAVA FIRST (IMPORTANT)
      - name: Setup Java 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      # Node
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Init project
      - run: npm init -y

      # Install all
      - run: npm install @capacitor/core @capacitor/cli @capacitor/android

      # Create web folder
      - run: mkdir www
      - run: cp index.html www/index.html

      # Init capacitor
      - run: npx cap init "Oni Short" oni.short.app --web-dir=www

      # Add android
      - run: npx cap add android || echo "already added"

      # Sync
      - run: npx cap sync

      # Clean cache
      - run: rm -rf ~/.gradle

      # Build APK
      - run: cd android && chmod +x gradlew && ./gradlew assembleDebug --no-daemon -Dorg.gradle.jvmargs="-Xmx512m"

      # Upload APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
